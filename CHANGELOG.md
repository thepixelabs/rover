# Changelog

All notable changes to Rover will be documented in this file.

## [Unreleased] - 2026-05-09

- Extracted from `thepixelabs/dispatch` repo. History preserved via `git filter-repo`.
- Removed `install.sh` — Homebrew (`thepixelabs/tap/rover`) and PyPI (`rover-tui`) are now the only install paths.
- Landing page moves to `rover.pixelabs.net`.
